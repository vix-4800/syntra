tools:
  phpstan:
    enabled: true
    level: 8
  phpunit:
    enabled: true
    config: "phpunit.xml"
  rector_refactorer:
    enabled: false
    config: "rector.php"
  php_cs_fixer_refactorer:
    enabled: false
    config: "php_cs_fixer.php"

checks:
  composer:
    enabled: true
  security:
    enabled: true

refactor:
  import_refactorer:
    enabled: true
  var_comments_refactorer:
    enabled: true
  docblock_refactorer:
    enabled: true

extensions:
  # -
