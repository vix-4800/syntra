tools:
  phpstan:
    enabled: true
    level: 8
  security_checker:
    enabled: true
  phpunit:
    enabled: true
    config: "phpunit.xml"

checks:
  composer:
    enabled: true

refactor:
  php_cs_fixer:
    enabled: false
    config: ".php_cs.dist"
  rector:
    enabled: false
    config: "rector.php"
  fix_imports:
    enabled: true
  fix_var_comments:
    enabled: true
  add_missing_docblocks:
    enabled: true

extensions:
  # -
