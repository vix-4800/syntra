tools:
  phpstan:
    enabled: true
    level: 8
  security_checker:
    enabled: true
  phpunit:
    enabled: true
    config: "phpunit.xml"
  rector:
    enabled: false
    config: "rector.php"
  php_cs_fixer:
    enabled: false
    config: "php_cs_fixer.php"

checks:
  composer:
    enabled: true

refactor:
  fix_imports:
    enabled: true
  fix_var_comments:
    enabled: true
  add_missing_docblocks:
    enabled: true

extensions:
  # -
